<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/app_background"
    android:padding="24dp">

    <!-- 전화번호 -->
    <TextView
        android:id="@+id/tvNumber"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="010-1234-5678"
        android:textColor="@color/main_text"
        android:textSize="26sp"
        android:textStyle="bold"
        android:ellipsize="end"
        android:maxLines="1"
        android:letterSpacing="0.02"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- 이름(또는 저장된 이름) -->
    <TextView
        android:id="@+id/name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="홍길동"
        android:textColor="@color/sub_text"
        android:textSize="18sp"
        android:maxLines="1"
        android:ellipsize="end"
        app:layout_constraintTop_toBottomOf="@id/tvNumber"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="8dp"/>

    <!-- 피싱 경고 카드 (기본: 숨김) -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/phishingBox"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:padding="14dp"
        android:background="@drawable/bg_phishing_card"
        android:elevation="8dp"
        app:layout_constraintTop_toBottomOf="@id/name"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@id/callSlideLayout"
        android:layout_marginTop="14dp">

        <!-- 경고 아이콘 -->
        <TextView
            android:id="@+id/tvWarnIcon"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:gravity="center"
            android:text="!"
            android:textStyle="bold"
            android:textSize="18sp"
            android:textColor="@color/pure_white"
            android:background="@drawable/bg_warn_circle"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"/>

        <!-- 타이틀 -->
        <TextView
            android:id="@+id/tvPhishingTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="의심 번호"
            android:textColor="@color/red"
            android:textStyle="bold"
            android:textSize="18sp"
            app:layout_constraintStart_toEndOf="@id/tvWarnIcon"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="@id/tvWarnIcon"
            android:layout_marginStart="10dp"/>

        <!-- 설명 -->
        <TextView
            android:id="@+id/tvPhishingDesc"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text=""
            android:textColor="@color/main_text"
            android:textSize="15sp"
            android:maxLines="2"
            android:ellipsize="end"
            app:layout_constraintStart_toStartOf="@id/tvPhishingTitle"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tvPhishingTitle"
            android:layout_marginTop="6dp"/>

        <!-- 신고 건수 (칩 느낌) -->
        <TextView
            android:id="@+id/tvReportCount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="신고 0건"
            android:textColor="@color/pure_white"
            android:textSize="13sp"
            android:paddingStart="10dp"
            android:paddingEnd="10dp"
            android:paddingTop="6dp"
            android:paddingBottom="6dp"
            android:background="@drawable/bg_chip_red"
            app:layout_constraintStart_toStartOf="@id/tvPhishingTitle"
            app:layout_constraintTop_toBottomOf="@id/tvPhishingDesc"
            android:layout_marginTop="10dp"/>

        <!-- 바로 차단 버튼 -->
        <Button
            android:id="@+id/btnBlockNow"
            android:layout_width="0dp"
            android:layout_height="44dp"
            android:text="바로 차단"
            android:textStyle="bold"
            android:textColor="@color/pure_white"
            android:background="@drawable/bg_block_button"
            android:insetLeft="0dp"
            android:insetRight="0dp"
            app:layout_constraintStart_toStartOf="@id/tvPhishingTitle"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tvReportCount"
            android:layout_marginTop="12dp"/>
    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- 🔥 슬라이드 수락/거절 영역 -->
    <androidx.constraintlayout.motion.widget.MotionLayout
        android:id="@+id/callSlideLayout"
        android:layout_width="match_parent"
        android:layout_height="140dp"
        app:layoutDescription="@xml/scene_call_slide"
        app:layout_constraintBottom_toBottomOf="parent">

        <TextView
            android:id="@+id/tvReject"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="거절"
            android:textColor="@color/red"
            android:textSize="20sp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"/>

        <TextView
            android:id="@+id/tvAccept"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="받기"
            android:textColor="@color/primary_blue"
            android:textSize="20sp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"/>

        <ImageView
            android:id="@+id/slider"
            android:layout_width="90dp"
            android:layout_height="90dp"
            android:background="@drawable/bg_slider"
            android:src="@drawable/ic_phone_call"
            android:padding="24dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:tint="@color/primary_blue" />

    </androidx.constraintlayout.motion.widget.MotionLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
